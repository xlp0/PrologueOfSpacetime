{
  "name": "document_search",
  "description": "Searches the 10,000+ local document repository for specific information. Use this when the user asks about deep knowledge, past protocols, or complex documents not in the immediate chat memory.",
  "parameters": {
    "type": "object",
    "properties": {
      "query": {
        "type": "string",
        "description": "The exact search query to look up in the vector database."
      }
    },
    "required": ["query"]
  },
  "command": {
    "type": "exec",
    "exec": "uv run /Users/Henrykoo/Documents/PrologueOfSpacetime/docs/workflows/openclaw_memory_scripts/doc_search.py --query \"{{query}}\""
  }
}
